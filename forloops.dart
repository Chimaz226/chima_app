void main() {
  // for (inti; condition; increment/decrement) {

  // }
  String value = "Hello";

  for (int i = 0; i < value.length; i++) {
    if (i != 1 && i != 2 && i != 3) {
      print(value[i]);
    }
  }

  // for (int i = 0; i < value.length; i++) {
  //   print(value[i]);
  // }

  // // while loop
  // /* while (condition){
  // }*/
  // int c = 0;
  // while (c < value.length) {
  //   print(value[c]);
  //   c = c + 1;
  // }

  // // Do While loops

  // int i = 0;
  // do {
  //   print(value[i]);
  //   i++;
  // } while (i <= value.length);
}
